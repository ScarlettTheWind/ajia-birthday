# 版本备份要求

用户要求保留每一个网站更新版本，禁止覆盖或删除已有版本备份。

- 开始修改网站前运行 `node backup-version.mjs 更新前快照`。
- 每次修改完成并检查通过后，运行 `node build-inline.mjs`。该命令生成页面并自动把完整版本另存到 `版本备份/`，不要跳过，纯样式修改也要执行。
- 仅修改非网站文件时，完成后运行 `node backup-version.mjs`。
- 备份包括网站、源码、说明、测试和项目配置；不包含凭据、Git 历史、依赖、压缩包或历史备份目录。
- 只有包含 `备份信息.json` 且 complete 为 true 的目录才是完成的备份。备份失败时先修复，不要声称已备份。
- 打包源码时包含 `backup-version.mjs` 和本文件，不把整个历史备份目录打包进网站或上传。
